# 🔍 ПОЛНЫЙ АУДИТ И ИСПРАВЛЕНИЕ АГЕНТА

## ЗАДАЧА

Провести полный аудит системы Autonomous Trading Agent, найти ВСЕ недостатки и проблемы, и устранить их согласно best practices.

---

## 📋 ЭТАП 1: ДИАГНОСТИКА СИСТЕМЫ

### 1.1 Проверка переменных окружения

**Проверить:**
- [ ] Существование `.env` файла
- [ ] Наличие всех обязательных переменных:
  - `QWEN_API_KEY`
  - `BYBIT_API_KEY`
  - `BYBIT_API_SECRET`
  - `TELEGRAM_BOT_TOKEN` (опционально, но желательно)
  - `TELEGRAM_CHAT_IDS` (опционально, но желательно)
- [ ] Корректность формата переменных
- [ ] Загрузка через `load_env.sh`

**Действия:**
1. Проверить наличие `.env` файла
2. Если нет - создать из `.env.example` или шаблона
3. Проверить что `load_env.sh` корректно загружает переменные
4. Убедиться что переменные доступны в Python окружении

### 1.2 Проверка зависимостей

**Проверить:**
- [ ] Все пакеты из `requirements.txt` установлены
- [ ] Версии совместимы
- [ ] Виртуальное окружение активировано
- [ ] Нет конфликтов зависимостей

**Команды для проверки:**
```bash
cd /Users/Gyber/GYBERNATY-ECOSYSTEM/TRADER-AGENT
source venv/bin/activate
pip list | grep -E "mcp|pybit|pandas|aiohttp|loguru"
pip check  # Проверка конфликтов
```

### 1.3 Проверка структуры проекта

**Проверить наличие всех ключевых файлов:**
- [ ] `autonomous_agent/main.py` - точка входа
- [ ] `autonomous_agent/autonomous_analyzer.py` - основной анализатор
- [ ] `autonomous_agent/qwen_client.py` - Qwen клиент
- [ ] `autonomous_agent/telegram_formatter.py` - форматирование
- [ ] `mcp_server/telegram_bot.py` - Telegram бот
- [ ] `publish_market_analysis.py` - публикация анализа
- [ ] `scripts/run_daily_analysis.sh` - скрипт запуска
- [ ] `config/` директория существует
- [ ] `data/` директория существует
- [ ] `logs/` директория существует

### 1.4 Проверка конфигурации

**Проверить:**
- [ ] `config/autonomous_agent.json` (если используется)
- [ ] `config/credentials.json` (если используется)
- [ ] Корректность путей в конфигурации
- [ ] Нет хардкода путей (особенно в `publish_market_analysis.py`)

### 1.5 Проверка автоматизации

**Проверить:**
- [ ] Настроен ли cron job для автоматического запуска
- [ ] Корректность пути к скрипту в cron
- [ ] Права на выполнение скриптов
- [ ] Логирование cron jobs

**Команды:**
```bash
crontab -l | grep -E "run_daily|autonomous"
ls -la scripts/run_daily_analysis.sh  # Проверка прав
```

---

## 📋 ЭТАП 2: ВЫЯВЛЕНИЕ ПРОБЛЕМ

### 2.1 Критические проблемы (блокируют работу)

**Найденные проблемы:**

1. **Отсутствие переменных окружения**
   - Симптом: `QWEN_API_KEY environment variable is required`
   - Причина: `.env` не загружен или переменные не установлены
   - Критичность: 🔴 КРИТИЧНО

2. **Хардкод путей в `publish_market_analysis.py`**
   - Симптом: Пути к файлам сканирования захардкожены
   - Причина: Использование абсолютных путей из `.cursor/projects/`
   - Критичность: 🟡 СРЕДНЕ (работает, но не portable)

3. **Отсутствие cron job**
   - Симптом: Агент не запускается автоматически
   - Причина: Cron job не настроен
   - Критичность: 🟡 СРЕДНЕ (можно запускать вручную)

### 2.2 Потенциальные проблемы

4. **Отсутствие проверки зависимостей при запуске**
   - Симптом: Ошибки импорта при отсутствии пакетов
   - Критичность: 🟢 НИЗКАЯ

5. **Отсутствие валидации конфигурации**
   - Симптом: Ошибки во время выполнения
   - Критичность: 🟢 НИЗКАЯ

6. **Отсутствие обработки ошибок в некоторых местах**
   - Симптом: Критические ошибки без graceful degradation
   - Критичность: 🟡 СРЕДНЯЯ

---

## 📋 ЭТАП 3: ИСПРАВЛЕНИЕ ПРОБЛЕМ

### 3.1 Исправление переменных окружения

**Действия:**

1. **Создать/проверить `.env` файл:**
```bash
# Если .env не существует, создать из шаблона
if [ ! -f .env ]; then
    cat > .env << 'EOF'
# Bybit API
BYBIT_API_KEY=your_bybit_api_key_here
BYBIT_API_SECRET=your_bybit_api_secret_here
BYBIT_TESTNET=false

# Qwen AI API (OpenRouter)
QWEN_API_KEY=your_qwen_api_key_here
QWEN_MODEL=qwen/qwen-turbo

# Telegram (опционально)
TELEGRAM_BOT_TOKEN=8003689195:AAGxQsopKvlLS34H2TZ0S1a0K7s4yV4iOBY
TELEGRAM_CHAT_IDS=-1003382613825,-1003484839912
EOF
fi
```

2. **Улучшить `load_env.sh`:**
   - Добавить проверку что переменные загружены
   - Добавить вывод статуса загрузки
   - Добавить проверку TELEGRAM переменных (опционально)

3. **Улучшить `autonomous_agent/main.py`:**
   - Добавить более детальные сообщения об ошибках
   - Добавить fallback на чтение из файла конфигурации
   - Добавить валидацию переменных

### 3.2 Исправление хардкода путей

**В файле `publish_market_analysis.py`:**

**Проблема:** Строки 23-27 содержат абсолютные пути:
```python
scan_files = [
    '/Users/Gyber/.cursor/projects/Users-Gyber-GYBERNATY-ECOSYSTEM-TRADER-AGENT/agent-tools/...',
    ...
]
```

**Решение:**
1. Использовать относительные пути от корня проекта
2. Или использовать переменные окружения
3. Или читать из `data/` директории
4. Добавить fallback если файлы не найдены

**Исправленный код:**
```python
from pathlib import Path

# Получаем корень проекта
PROJECT_ROOT = Path(__file__).parent

# Используем data директорию для результатов сканирования
scan_files = [
    PROJECT_ROOT / "data" / "scan_results_1.json",
    PROJECT_ROOT / "data" / "scan_results_2.json",
    PROJECT_ROOT / "data" / "scan_results_3.json",
]

# Или читать последние N файлов из data/
data_dir = PROJECT_ROOT / "data"
scan_files = sorted(
    data_dir.glob("scan_results_*.json"),
    key=lambda p: p.stat().st_mtime,
    reverse=True
)[:3]  # Последние 3 файла
```

### 3.3 Настройка автоматизации

**Действия:**

1. **Создать улучшенный скрипт установки cron:**
```bash
#!/bin/bash
# scripts/setup_autonomous_agent_cron.sh

PROJECT_DIR="/Users/Gyber/GYBERNATY-ECOSYSTEM/TRADER-AGENT"
SCRIPT_PATH="$PROJECT_DIR/scripts/run_daily_analysis.sh"

# Проверка существования скрипта
if [ ! -f "$SCRIPT_PATH" ]; then
    echo "❌ Script not found: $SCRIPT_PATH"
    exit 1
fi

# Делаем исполняемым
chmod +x "$SCRIPT_PATH"

# Время запуска (по умолчанию каждые 4 часа)
SCHEDULE="${1:-0 */4 * * *}"  # Каждые 4 часа

# Создаём временный файл
CRON_TEMP=$(mktemp)
crontab -l > "$CRON_TEMP" 2>/dev/null || true

# Удаляем старые задачи
grep -v "run_daily_analysis.sh" "$CRON_TEMP" > "${CRON_TEMP}.new" || true
mv "${CRON_TEMP}.new" "$CRON_TEMP"

# Добавляем новую задачу
echo "$SCHEDULE $SCRIPT_PATH >> $PROJECT_DIR/logs/cron.log 2>&1" >> "$CRON_TEMP"

# Устанавливаем
crontab "$CRON_TEMP"
rm "$CRON_TEMP"

echo "✅ Cron job установлен!"
echo "📅 Расписание: $SCHEDULE"
echo ""
echo "Текущие cron jobs:"
crontab -l | grep "run_daily_analysis"
```

2. **Улучшить `scripts/run_daily_analysis.sh`:**
   - Добавить более детальное логирование
   - Добавить проверку успешности публикации в Telegram
   - Добавить отправку уведомлений об ошибках
   - Добавить таймауты для операций

### 3.4 Добавление проверки зависимостей

**Создать скрипт `scripts/check_dependencies.py`:**
```python
#!/usr/bin/env python3
"""Проверка всех зависимостей перед запуском агента"""

import sys
import importlib

REQUIRED_PACKAGES = [
    'mcp',
    'pybit',
    'pandas',
    'numpy',
    'aiohttp',
    'loguru',
    'ta',
]

missing = []
for package in REQUIRED_PACKAGES:
    try:
        importlib.import_module(package)
    except ImportError:
        missing.append(package)

if missing:
    print(f"❌ Отсутствуют пакеты: {', '.join(missing)}")
    print(f"Установите: pip install {' '.join(missing)}")
    sys.exit(1)
else:
    print("✅ Все зависимости установлены")
    sys.exit(0)
```

### 3.5 Улучшение обработки ошибок

**В `autonomous_agent/main.py`:**
- Добавить try-except блоки для всех критических операций
- Добавить graceful degradation (продолжать работу при некритичных ошибках)
- Добавить детальное логирование ошибок
- Добавить отправку уведомлений об критических ошибках

---

## 📋 ЭТАП 4: ТЕСТИРОВАНИЕ

### 4.1 Тест переменных окружения

```bash
source load_env.sh
python -c "import os; print('QWEN_API_KEY:', 'SET' if os.getenv('QWEN_API_KEY') else 'NOT SET')"
```

### 4.2 Тест зависимостей

```bash
source venv/bin/activate
python scripts/check_dependencies.py
```

### 4.3 Тест Telegram публикации

```bash
source venv/bin/activate
python test_telegram_send.py
```

### 4.4 Тест запуска агента

```bash
source venv/bin/activate
source load_env.sh
python -m autonomous_agent.main
```

### 4.5 Тест автоматизации

```bash
# Ручной запуск скрипта
./scripts/run_daily_analysis.sh

# Проверка логов
tail -f logs/daily_analysis_$(date +%Y%m%d).log
```

---

## 📋 ЭТАП 5: ДОКУМЕНТАЦИЯ И МОНИТОРИНГ

### 5.1 Создать скрипт проверки статуса

**`scripts/check_agent_status.sh`:**
```bash
#!/bin/bash
# Проверка статуса агента

echo "🔍 Проверка статуса Autonomous Agent..."
echo ""

# Проверка переменных окружения
echo "📋 Переменные окружения:"
source load_env.sh 2>/dev/null
[ -n "$QWEN_API_KEY" ] && echo "  ✅ QWEN_API_KEY: SET" || echo "  ❌ QWEN_API_KEY: NOT SET"
[ -n "$BYBIT_API_KEY" ] && echo "  ✅ BYBIT_API_KEY: SET" || echo "  ❌ BYBIT_API_KEY: NOT SET"
[ -n "$TELEGRAM_BOT_TOKEN" ] && echo "  ✅ TELEGRAM_BOT_TOKEN: SET" || echo "  ⚠️  TELEGRAM_BOT_TOKEN: NOT SET (optional)"

echo ""
echo "📦 Зависимости:"
source venv/bin/activate
python scripts/check_dependencies.py

echo ""
echo "⏰ Cron jobs:"
crontab -l | grep "run_daily_analysis" && echo "  ✅ Cron job установлен" || echo "  ❌ Cron job не установлен"

echo ""
echo "📊 Последний анализ:"
if [ -f "data/latest_analysis.json" ]; then
    echo "  ✅ Файл существует"
    echo "  📅 Время: $(jq -r '.timestamp' data/latest_analysis.json 2>/dev/null || echo 'unknown')"
else
    echo "  ❌ Файл не найден"
fi

echo ""
echo "📱 Telegram публикация:"
if [ -f "data/latest_telegram_message.txt" ]; then
    echo "  ✅ Сообщение готово"
    echo "  📏 Размер: $(wc -l < data/latest_telegram_message.txt) строк"
else
    echo "  ⚠️  Сообщение не найдено"
fi
```

### 5.2 Создать README с инструкциями

**`AUTONOMOUS_AGENT_TROUBLESHOOTING.md`**

---

## ✅ КРИТЕРИИ УСПЕХА

После выполнения всех исправлений:

- [ ] ✅ Агент запускается без ошибок: `python -m autonomous_agent.main`
- [ ] ✅ Telegram публикация работает: `python test_telegram_send.py`
- [ ] ✅ Все переменные окружения загружаются корректно
- [ ] ✅ Cron job установлен и работает
- [ ] ✅ Нет хардкода путей в коде
- [ ] ✅ Все зависимости установлены
- [ ] ✅ Логирование работает корректно
- [ ] ✅ Обработка ошибок улучшена

---

## 🚀 ПОРЯДОК ВЫПОЛНЕНИЯ

1. **Сначала диагностика** - найти все проблемы
2. **Затем исправления** - устранить по приоритету (критичные → средние → низкие)
3. **Потом тестирование** - проверить каждое исправление
4. **Наконец документация** - обновить инструкции

---

## 📝 ПРИМЕЧАНИЯ

- Все изменения должны быть backward compatible
- Использовать best practices для Python проектов
- Сохранять существующую функциональность
- Добавлять детальное логирование
- Улучшать обработку ошибок
- Делать код более portable (без хардкода путей)

---

**Версия:** 1.0  
**Дата:** 2025-11-20  
**Статус:** Готов к выполнению

