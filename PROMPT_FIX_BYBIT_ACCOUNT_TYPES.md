# 🔧 ПРОМПТ: Исправление проблемы с типами счетов Bybit

## 🎯 ЦЕЛЬ

Исправить проблему с получением баланса и торговыми операциями на Bybit, связанную с неправильной работой с разными типами счетов (SPOT, CONTRACT, UNIFIED).

---

## 📋 КОНТЕКСТ ПРОБЛЕМЫ

### Текущая ситуация:
- **Депозит:** $30 USD
- **Проблема:** `get_account_info` показывает `available: 0.0`, хотя `total: 30.0`
- **Причина:** Код проверяет только `UNIFIED` счет, а средства могут быть на `SPOT` счете

### Текущая реализация:
1. `get_account_info` в `full_server.py:520` использует только `accountType="UNIFIED"`
2. `close_position` в `trading_operations.py:252` пробует `SPOT`, затем `UNIFIED`, но логика неполная
3. Нет единой стратегии работы с разными типами счетов

---

## 🔍 ЗАДАЧИ ДЛЯ ВЫПОЛНЕНИЯ

### 1. ИЗУЧИТЬ СТРУКТУРУ СЧЕТОВ BYBIT

**Что нужно изучить:**
- [ ] Типы счетов Bybit: `SPOT`, `CONTRACT`, `UNIFIED`
- [ ] Различия между типами счетов
- [ ] Как средства распределяются между счетами
- [ ] API endpoints для каждого типа счета
- [ ] Как правильно получать баланс для каждого типа

**Где искать информацию:**
- `bybit-mcp/src/tools/GetWalletBalance.ts` - пример работы с accountType
- `bybit-mcp/specs/bybit/bybit-api-v5-openapi.yaml` - официальная документация API
- `bybit-mcp/specs/bybit/bybit-api-v5-postman-collection.json` - примеры запросов
- `mcp_server/trading_operations.py` - текущая реализация
- `mcp_server/full_server.py` - обработка get_account_info

**Ключевые вопросы:**
1. Что такое UNIFIED счет и как он связан с SPOT/CONTRACT?
2. Могут ли средства быть только на SPOT, только на CONTRACT, или только на UNIFIED?
3. Как правильно определить, на каком счете находятся средства?
4. Нужно ли проверять все типы счетов или есть приоритет?

---

### 2. ПРОАНАЛИЗИРОВАТЬ ТЕКУЩИЙ КОД

**Файлы для анализа:**

#### `mcp_server/full_server.py` (строки 516-580)
- [ ] Как сейчас реализован `get_account_info`
- [ ] Какой `accountType` используется
- [ ] Как обрабатываются ошибки
- [ ] Что возвращается пользователю

#### `mcp_server/trading_operations.py` (строки 240-350)
- [ ] Как `close_position` определяет тип счета
- [ ] Логика fallback (SPOT → UNIFIED)
- [ ] Как `place_order` определяет тип счета
- [ ] Есть ли валидация типа счета для разных категорий (spot/linear/inverse)

**Ключевые вопросы:**
1. Почему используется только UNIFIED в `get_account_info`?
2. Правильно ли работает fallback в `close_position`?
3. Нужно ли проверять все типы счетов при получении баланса?
4. Как связаны `category` (spot/linear/inverse) и `accountType` (SPOT/CONTRACT/UNIFIED)?

---

### 3. ИЗУЧИТЬ BYBIT API ДОКУМЕНТАЦИЮ

**Что нужно найти:**

#### API Endpoint: `/v5/account/wallet-balance`
- [ ] Все возможные значения `accountType`
- [ ] Структура ответа для каждого типа
- [ ] Различия в полях ответа между типами
- [ ] Как интерпретировать `walletBalance`, `availableToWithdraw`, `availableBalance`

#### Связь между accountType и category:
- [ ] `category="spot"` → какой `accountType` использовать?
- [ ] `category="linear"` → какой `accountType` использовать?
- [ ] `category="inverse"` → какой `accountType` использовать?
- [ ] Можно ли использовать UNIFIED для всех категорий?

**Где искать:**
- `bybit-mcp/specs/bybit/bybit-api-v5-openapi.yaml` - полная спецификация
- `bybit-mcp/src/tools/GetWalletBalance.ts` - пример реализации
- Официальная документация Bybit API v5

---

### 4. РАЗРАБОТАТЬ СТРАТЕГИЮ РЕШЕНИЯ

**Варианты решения:**

#### Вариант A: Проверять все типы счетов
- Проверять SPOT, CONTRACT, UNIFIED последовательно
- Суммировать балансы или показывать по отдельности
- **Плюсы:** Полная информация
- **Минусы:** Больше API запросов

#### Вариант B: Использовать правильный accountType по category
- `category="spot"` → `accountType="SPOT"`
- `category="linear"` → `accountType="CONTRACT"` или `"UNIFIED"`
- **Плюсы:** Точное соответствие
- **Минусы:** Нужно знать category заранее

#### Вариант C: Использовать UNIFIED как основной, с fallback
- Сначала проверять UNIFIED
- Если баланс 0, проверять SPOT и CONTRACT
- **Плюсы:** Минимум запросов
- **Минусы:** Может пропустить средства на отдельных счетах

**Рекомендация:** Комбинированный подход - использовать правильный accountType по category, с fallback на другие типы если баланс 0.

---

### 5. РЕАЛИЗОВАТЬ ИСПРАВЛЕНИЯ

**Что нужно исправить:**

#### 5.1. `get_account_info` в `full_server.py`

**Текущая проблема:**
```python
wallet_response = trading_ops.session.get_wallet_balance(accountType="UNIFIED")
```

**Что нужно сделать:**
- [ ] Проверять все типы счетов: SPOT, CONTRACT, UNIFIED
- [ ] Показывать баланс для каждого типа отдельно
- [ ] Или суммировать, если это имеет смысл
- [ ] Правильно обрабатывать случаи, когда счет не существует

**Ожидаемый результат:**
```python
{
  "balance": {
    "spot": {"total": 30.0, "available": 30.0},
    "contract": {"total": 0.0, "available": 0.0},
    "unified": {"total": 0.0, "available": 0.0},
    "total": 30.0,
    "available": 30.0
  }
}
```

#### 5.2. `place_order` в `trading_operations.py`

**Что нужно сделать:**
- [ ] Определять правильный `accountType` на основе `category`
- [ ] Для `category="spot"` → использовать `accountType="SPOT"` (или проверить UNIFIED)
- [ ] Для `category="linear"` → использовать `accountType="CONTRACT"` или `"UNIFIED"`
- [ ] Валидировать, что средства доступны на нужном счете

#### 5.3. `close_position` в `trading_operations.py`

**Текущая реализация:**
```python
wallet_response = self.session.get_wallet_balance(accountType="SPOT", coin=base_coin)
# Если не получилось, пробуем UNIFIED
```

**Что нужно улучшить:**
- [ ] Правильно определять accountType по category
- [ ] Улучшить логику fallback
- [ ] Добавить логирование для отладки
- [ ] Обрабатывать все возможные случаи

#### 5.4. Добавить вспомогательную функцию

**Создать функцию:**
```python
def get_account_type_for_category(category: str) -> str:
    """
    Определяет правильный accountType для category
    
    Args:
        category: "spot", "linear", "inverse"
    
    Returns:
        "SPOT", "CONTRACT", или "UNIFIED"
    """
```

**Использовать везде, где нужно определить accountType.**

---

### 6. ПРОТЕСТИРОВАТЬ РЕШЕНИЕ

**Тестовые сценарии:**

#### Сценарий 1: Средства на SPOT счете
- [ ] `get_account_info` должен показать баланс на SPOT
- [ ] `place_order` с `category="spot"` должен работать
- [ ] `close_position` с `category="spot"` должен работать

#### Сценарий 2: Средства на UNIFIED счете
- [ ] `get_account_info` должен показать баланс на UNIFIED
- [ ] Торговые операции должны работать

#### Сценарий 3: Средства на CONTRACT счете
- [ ] `get_account_info` должен показать баланс на CONTRACT
- [ ] `place_order` с `category="linear"` должен работать

#### Сценарий 4: Средства распределены между счетами
- [ ] `get_account_info` должен показать балансы всех счетов
- [ ] Правильно определять доступные средства для каждой категории

---

### 7. ДОКУМЕНТИРОВАТЬ ИЗМЕНЕНИЯ

**Что нужно задокументировать:**

- [ ] Как работают разные типы счетов Bybit
- [ ] Какое соответствие между `category` и `accountType`
- [ ] Как правильно получать баланс для каждой категории
- [ ] Примеры использования исправленного кода
- [ ] Возможные edge cases и как их обрабатывать

**Создать файл:**
- `docs/BYBIT_ACCOUNT_TYPES_GUIDE.md` - руководство по типам счетов

---

## 📚 РЕСУРСЫ ДЛЯ ИЗУЧЕНИЯ

### Код проекта:
1. `mcp_server/full_server.py` - обработка get_account_info
2. `mcp_server/trading_operations.py` - торговые операции
3. `bybit-mcp/src/tools/GetWalletBalance.ts` - пример работы с accountType
4. `bybit-mcp/specs/bybit/bybit-api-v5-openapi.yaml` - спецификация API

### Документация:
1. Bybit API v5 Documentation
2. Pybit library documentation (если используется)
3. Примеры из `bybit-mcp/specs/bybit/bybit-api-v5-postman-collection.json`

---

## ✅ КРИТЕРИИ УСПЕХА

Решение считается успешным, если:

1. ✅ `get_account_info` показывает правильный баланс для всех типов счетов
2. ✅ `place_order` с `category="spot"` работает, если средства на SPOT счете
3. ✅ `close_position` правильно определяет тип счета и закрывает позицию
4. ✅ Код обрабатывает все возможные комбинации счетов
5. ✅ Добавлена документация по типам счетов
6. ✅ Все тестовые сценарии проходят успешно

---

## 🎯 ИНСТРУКЦИИ ДЛЯ ВЫПОЛНЕНИЯ

### Шаг 1: Изучение (30-40 минут)
1. Прочитать документацию Bybit API v5 о типах счетов
2. Изучить текущий код в проекте
3. Понять различия между SPOT, CONTRACT, UNIFIED
4. Определить правильное соответствие category → accountType

### Шаг 2: Анализ (20-30 минут)
1. Найти все места, где используется accountType
2. Определить проблемы в текущей реализации
3. Разработать стратегию исправления
4. Выбрать лучший подход

### Шаг 3: Реализация (40-60 минут)
1. Исправить `get_account_info` для проверки всех типов счетов
2. Исправить `place_order` для правильного определения accountType
3. Улучшить `close_position` с правильной логикой fallback
4. Добавить вспомогательные функции
5. Добавить логирование для отладки

### Шаг 4: Тестирование (20-30 минут)
1. Протестировать все сценарии
2. Убедиться, что баланс показывается правильно
3. Проверить торговые операции
4. Исправить найденные баги

### Шаг 5: Документация (15-20 минут)
1. Создать руководство по типам счетов
2. Обновить комментарии в коде
3. Добавить примеры использования

---

## 🔍 ВАЖНЫЕ ВОПРОСЫ ДЛЯ ОТВЕТА

Перед началом реализации ответь на эти вопросы:

1. **Что такое UNIFIED счет в Bybit?**
   - Это объединенный счет для всех категорий?
   - Или это отдельный тип счета?
   - Могут ли средства быть одновременно на UNIFIED и SPOT?

2. **Как правильно получать баланс для spot торговли?**
   - Нужно проверять SPOT или UNIFIED?
   - Или оба?
   - Есть ли приоритет?

3. **Как связаны category и accountType?**
   - `category="spot"` всегда требует `accountType="SPOT"`?
   - Или можно использовать UNIFIED для spot?
   - Как это работает в реальности?

4. **Что делать, если средства на разных счетах?**
   - Показывать все балансы отдельно?
   - Или суммировать?
   - Как определять доступные средства для торговли?

---

## 💡 ПОДСКАЗКИ

1. **Начни с изучения `GetWalletBalance.ts`** - там есть пример работы с accountType
2. **Проверь Postman коллекцию** - там есть примеры запросов для разных accountType
3. **Используй логирование** - добавь детальные логи для понимания, что происходит
4. **Тестируй постепенно** - сначала исправь `get_account_info`, затем торговые операции
5. **Документируй решения** - записывай найденную информацию о типах счетов

---

## 🚀 НАЧНИ С ЭТОГО

1. Открой `bybit-mcp/src/tools/GetWalletBalance.ts` и изучи как там работает accountType
2. Открой `bybit-mcp/specs/bybit/bybit-api-v5-openapi.yaml` и найди документацию по `/v5/account/wallet-balance`
3. Прочитай текущий код в `mcp_server/full_server.py:516-580`
4. Пойми проблему и разработай решение
5. Реализуй исправления
6. Протестируй и задокументируй

---

**Удачи! Разберись основательно и реши проблему правильно!** 🎯



