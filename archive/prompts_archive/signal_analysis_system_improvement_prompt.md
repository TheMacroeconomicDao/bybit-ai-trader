# 🔬 ПРОМПТ: Анализ и Улучшение Системы Анализа Сигналов

## ЗАДАЧА

Ты - эксперт по анализу торговых систем и машинному обучению. Твоя задача - **глубоко проанализировать реализованную систему анализа сигналов**, найти **корневые причины ошибок** и **предложить конкретные улучшения** на основе реальных результатов.

---

## КОНТЕКСТ

### Реализованная Система

**Файлы для анализа:**
- `mcp_server/market_scanner.py` - функция `_calculate_opportunity_score()` (строки 308-468)
- `mcp_server/technical_analysis.py` - функция `validate_entry()` (строки 695-783)
- `mcp_server/market_scanner.py` - функция `_estimate_probability()` (строки 470-480)
- `prompts/entry_decision_framework.md` - теоретическая матрица scoring (10 факторов)

### Реальные Результаты

**Файлы с реальными ошибками:**
- `ANALYSIS_FAILURE_REPORT.md` - детальный разбор провала ZEN/USDT сделки
- История сделок показывает несоответствие между score/probability и реальными результатами

---

## ЭТАП 1: АНАЛИЗ ТЕКУЩЕЙ РЕАЛИЗАЦИИ

### ШАГ 1.1: Изучи Реализацию Scoring

**Прочитай и проанализируй:**

1. `mcp_server/market_scanner.py` - функция `_calculate_opportunity_score()`:
   - Какие 10 факторов учитываются?
   - Как рассчитывается каждый фактор?
   - Какие веса у каждого фактора?
   - Есть ли логические ошибки?

2. `mcp_server/market_scanner.py` - функция `_estimate_probability()`:
   - Как рассчитывается вероятность?
   - Учитывается ли composite signal confidence?
   - Корректна ли формула?

3. `mcp_server/technical_analysis.py` - функция `validate_entry()`:
   - Какие проверки выполняются?
   - Как рассчитывается финальный score?
   - Соответствует ли он scoring матрице?

### ШАГ 1.2: Сравни с Теорией

**Сравни реализацию с:**
- `prompts/entry_decision_framework.md` - теоретическая матрица
- `knowledge_base/7_zero_risk_methodology.md` - методология zero risk

**Найди несоответствия:**
- Отсутствующие факторы
- Неправильные веса
- Логические ошибки
- Недостающие проверки

---

## ЭТАП 2: АНАЛИЗ РЕАЛЬНЫХ ОШИБОК

### ШАГ 2.1: Изучи Провал ZEN/USDT

**Прочитай `ANALYSIS_FAILURE_REPORT.md` и ответь:**

1. **Какие факторы система НЕ учла:**
   - MACD bearish crossover (игнорирован)
   - Низкий volume (не проверен)
   - Composite signal = HOLD (проигнорирован)
   - BB Squeeze без volume (не учтён)

2. **Почему система дала score 8.46 и probability 95%:**
   - Какие факторы дали высокий score?
   - Почему они перевесили bearish сигналы?
   - Где логическая ошибка в scoring?

3. **Что должно было остановить вход:**
   - Какие проверки отсутствуют?
   - Какие факторы должны иметь больше веса?
   - Какие факторы должны быть обязательными?

### ШАГ 2.2: Найди Корневые Причины

**Проанализируй:**

1. **Почему MACD bearish не снизил score:**
   - Учитывается ли MACD direction в scoring?
   - Есть ли penalty за bearish MACD?
   - Правильно ли работает composite signal?

2. **Почему низкий volume не остановил:**
   - Какие требования к volume для скальпинга?
   - Учитывается ли volume на коротких TF (1m, 5m)?
   - Есть ли минимальный threshold для volume?

3. **Почему Composite Signal = HOLD проигнорирован:**
   - Как composite signal влияет на финальный score?
   - Должен ли HOLD сигнал блокировать вход?
   - Как confidence влияет на решение?

---

## ЭТАП 3: ВЫЯВЛЕНИЕ СИСТЕМНЫХ ПРОБЛЕМ

### ПРОБЛЕМА #1: Недостаточная Важность Composite Signal

**Вопросы:**
- Как composite signal учитывается в scoring?
- Должен ли HOLD сигнал давать score = 0?
- Должен ли confidence < 0.5 блокировать вход?

**Анализ:**
- Проверь строки 316-327 в `market_scanner.py`
- Проверь как composite signal влияет на финальный score
- Найди где composite signal игнорируется

### ПРОБЛЕМА #2: Volume Не Учитывается Достаточно

**Вопросы:**
- Учитывается ли volume на коротких TF (1m, 5m) для скальпинга?
- Есть ли минимальный threshold для volume?
- Должен ли низкий volume давать penalty или блокировать вход?

**Анализ:**
- Проверь строки 356-365 в `market_scanner.py`
- Проверь используется ли volume_ratio с коротких TF
- Найди где volume проверяется недостаточно строго

### ПРОБЛЕМА #3: MACD Direction Не Учитывается

**Вопросы:**
- Учитывается ли MACD crossover direction в scoring?
- Есть ли penalty за bearish MACD на коротких TF?
- Должен ли bearish MACD блокировать LONG вход?

**Анализ:**
- Проверь как MACD учитывается в composite signal
- Проверь учитывается ли MACD direction отдельно
- Найди где MACD bearish игнорируется

### ПРОБЛЕМА #4: Probability Завышена

**Вопросы:**
- Почему probability была 95% при confidence 0.15-0.33?
- Корректна ли формула в `_estimate_probability()`?
- Должна ли probability учитывать composite confidence?

**Анализ:**
- Проверь строки 470-480 в `market_scanner.py`
- Проверь формулу: `base_prob * (0.7 + confidence * 0.6)`
- Найди где probability завышается

### ПРОБЛЕМА #5: Отсутствие Обязательных Проверок

**Вопросы:**
- Есть ли обязательные факторы которые должны блокировать вход?
- Проверяется ли composite signal = HOLD?
- Проверяется ли volume для скальпинга?

**Анализ:**
- Проверь `validate_entry()` - какие обязательные проверки?
- Проверь есть ли hard stops (блокирующие вход)
- Найди где отсутствуют критичные проверки

---

## ЭТАП 4: ПРЕДЛОЖЕНИЕ УЛУЧШЕНИЙ

### УЛУЧШЕНИЕ #1: Обязательная Проверка Composite Signal

**Проблема:**
Composite signal = HOLD с confidence < 0.5 должен блокировать вход.

**Решение:**
```python
# В _calculate_opportunity_score() или validate_entry()
composite = analysis.get('composite_signal', {})
signal = composite.get('signal', 'HOLD')
confidence = composite.get('confidence', 0.5)

# HARD STOP: Если HOLD с низкой confidence - блокируем
if signal == 'HOLD' and confidence < 0.5:
    return {"total": 0.0, "breakdown": {...}, "blocked": True, "reason": "Composite signal HOLD with low confidence"}

# PENALTY: Если HOLD с средней confidence - большой penalty
if signal == 'HOLD':
    score_multiplier = confidence  # 0.5 = 50% от score
    return {"total": score * score_multiplier, ...}
```

### УЛУЧШЕНИЕ #2: Volume Проверка для Скальпинга

**Проблема:**
Низкий volume на коротких TF не учитывается для скальпинга.

**Решение:**
```python
# Добавить проверку volume на коротких TF
def _check_scalping_volume(self, analysis: Dict) -> Dict:
    """Проверка volume для скальпинга"""
    volume_checks = {}
    
    # Проверяем volume на всех коротких TF
    for tf in ['1m', '5m', '15m']:
        tf_data = analysis.get('timeframes', {}).get(tf, {})
        vol_ratio = tf_data.get('indicators', {}).get('volume', {}).get('volume_ratio', 0)
        volume_checks[tf] = vol_ratio
        
        # HARD STOP для скальпинга: volume < 0.5 на 1m или 5m
        if tf in ['1m', '5m'] and vol_ratio < 0.5:
            return {"passed": False, "reason": f"Volume too low on {tf}: {vol_ratio}"}
    
    # Минимум для скальпинга: хотя бы один TF с volume > 1.0
    max_vol = max([volume_checks.get(tf, 0) for tf in ['1m', '5m', '15m']])
    if max_vol < 1.0:
        return {"passed": False, "reason": f"All TF have low volume, max: {max_vol}"}
    
    return {"passed": True, "volume_ratios": volume_checks}
```

### УЛУЧШЕНИЕ #3: MACD Direction Penalty

**Проблема:**
Bearish MACD на коротких TF не даёт penalty для LONG.

**Решение:**
```python
# В _calculate_opportunity_score() добавить MACD penalty
def _check_macd_alignment(self, analysis: Dict, is_long: bool) -> float:
    """Проверка MACD alignment на коротких TF"""
    penalty = 0.0
    
    # Проверяем MACD на коротких TF (1m, 5m, 15m)
    for tf in ['1m', '5m', '15m']:
        tf_data = analysis.get('timeframes', {}).get(tf, {})
        macd = tf_data.get('indicators', {}).get('macd', {})
        crossover = macd.get('crossover', 'neutral')
        
        # Penalty за противоречие
        if is_long and crossover == 'bearish':
            penalty += 0.5  # Каждый bearish MACD = -0.5
        elif not is_long and crossover == 'bullish':
            penalty += 0.5
    
    # Если 2+ TF показывают противоречие - блокируем
    if penalty >= 1.0:
        return -10.0  # Блокируем вход
    
    return -penalty  # Возвращаем penalty
```

### УЛУЧШЕНИЕ #4: Исправление Probability Formula

**Проблема:**
Probability завышена, не учитывает composite confidence правильно.

**Решение:**
```python
def _estimate_probability(self, score: float, analysis: Dict) -> float:
    """Оценка вероятности успеха - ИСПРАВЛЕННАЯ"""
    
    composite = analysis.get('composite_signal', {})
    signal = composite.get('signal', 'HOLD')
    confidence = composite.get('confidence', 0.5)
    
    # HARD STOP: Если HOLD или низкая confidence
    if signal == 'HOLD' or confidence < 0.4:
        return 0.0  # Нулевая вероятность!
    
    # Базовая вероятность от score
    base_prob = 0.5 + (score - 5) * 0.05
    
    # КРИТИЧНО: Confidence - это MULTIPLIER, не additive
    # Если confidence = 0.5, то probability должна быть 50% от base
    adjusted_prob = base_prob * confidence
    
    # Дополнительная корректировка на composite score
    comp_score = abs(composite.get('score', 0))
    if comp_score < 3:
        adjusted_prob *= 0.7  # Ещё больше снижаем
    
    return round(min(0.95, max(0.0, adjusted_prob)), 2)
```

### УЛУЧШЕНИЕ #5: Обязательные Hard Stops

**Проблема:**
Нет обязательных проверок которые блокируют вход.

**Решение:**
```python
def _check_hard_stops(self, analysis: Dict, entry_plan: Dict, is_long: bool) -> Dict:
    """Обязательные проверки которые БЛОКИРУЮТ вход"""
    
    stops = []
    blocked = False
    
    composite = analysis.get('composite_signal', {})
    
    # STOP #1: Composite Signal = HOLD с низкой confidence
    if composite.get('signal') == 'HOLD' and composite.get('confidence', 0.5) < 0.5:
        stops.append("Composite signal HOLD with confidence < 0.5")
        blocked = True
    
    # STOP #2: MACD bearish на 2+ коротких TF для LONG
    if is_long:
        bearish_count = 0
        for tf in ['1m', '5m', '15m']:
            macd = analysis.get('timeframes', {}).get(tf, {}).get('indicators', {}).get('macd', {})
            if macd.get('crossover') == 'bearish':
                bearish_count += 1
        if bearish_count >= 2:
            stops.append(f"MACD bearish on {bearish_count} short timeframes")
            blocked = True
    
    # STOP #3: Volume слишком низкий для скальпинга
    volume_check = self._check_scalping_volume(analysis)
    if not volume_check.get('passed', True):
        stops.append(volume_check.get('reason', 'Volume too low'))
        blocked = True
    
    # STOP #4: BB Squeeze без volume confirmation
    # (если есть squeeze, но volume < 0.5)
    
    return {
        "blocked": blocked,
        "stops": stops,
        "can_proceed": not blocked
    }
```

---

## ЭТАП 5: СОЗДАНИЕ УЛУЧШЕННОЙ ВЕРСИИ

### Задача

Создай улучшенную версию функций:

1. **`_calculate_opportunity_score_v2()`** - с учётом всех улучшений
2. **`_estimate_probability_v2()`** - исправленная формула
3. **`_check_hard_stops()`** - обязательные проверки
4. **`_check_scalping_volume()`** - проверка volume для скальпинга
5. **`_check_macd_alignment()`** - проверка MACD alignment

### Требования

**Все улучшения должны:**
- ✅ Учитывать composite signal как приоритет
- ✅ Проверять volume на коротких TF для скальпинга
- ✅ Давать penalty за bearish MACD
- ✅ Блокировать вход при HOLD с низкой confidence
- ✅ Корректно рассчитывать probability
- ✅ Иметь обязательные hard stops

---

## ЭТАП 6: ВАЛИДАЦИЯ УЛУЧШЕНИЙ

### Тест на ZEN/USDT Сделке

**Примени улучшенную систему к ZEN/USDT (момент входа 22:32):**

1. **Composite Signal:** HOLD, confidence 0.15-0.33
   - Должен ли блокировать? ✅ ДА
   - Блокирует ли новая система? ✅ ДА

2. **Volume:** 0.11 на 1m, 0.04 на 5m
   - Должен ли блокировать? ✅ ДА
   - Блокирует ли новая система? ✅ ДА

3. **MACD:** Bearish на 1m и 5m
   - Должен ли блокировать? ✅ ДА
   - Блокирует ли новая система? ✅ ДА

**Результат:**
- Старая система: score 8.46, probability 95% → ВХОД ❌
- Новая система: BLOCKED (3 hard stops) → НЕТ ВХОДА ✅

---

## ЭТАП 7: ДОКУМЕНТАЦИЯ УЛУЧШЕНИЙ

### Создай Документ

**Создай файл:** `SYSTEM_IMPROVEMENTS_SIGNAL_ANALYSIS.md`

**Содержание:**
1. Анализ текущей реализации
2. Найденные проблемы
3. Корневые причины
4. Предложенные улучшения (с кодом)
5. Тестирование на реальных данных
6. План внедрения

**Формат:**
- Детальный анализ каждого компонента
- Конкретный код улучшений
- Примеры до/после
- Метрики улучшения

---

## ИНСТРУКЦИИ ДЛЯ ВЫПОЛНЕНИЯ

### ШАГ 1: Прочитай Все Файлы

```
1. mcp_server/market_scanner.py (строки 308-480)
2. mcp_server/technical_analysis.py (строки 695-783)
3. ANALYSIS_FAILURE_REPORT.md (полностью)
4. prompts/entry_decision_framework.md
5. knowledge_base/7_zero_risk_methodology.md
```

### ШАГ 2: Проанализируй

**Ответь на вопросы:**
1. Почему система дала score 8.46 для ZEN/USDT?
2. Какие факторы перевесили bearish сигналы?
3. Где логические ошибки в scoring?
4. Почему probability была 95% при confidence 0.15?

### ШАГ 3: Предложи Улучшения

**Создай:**
1. Улучшенные функции с кодом
2. Документ с анализом и улучшениями
3. План внедрения

### ШАГ 4: Валидируй

**Проверь на ZEN/USDT:**
- Блокирует ли новая система вход?
- Корректны ли новые score и probability?
- Работают ли hard stops?

---

## КРИТЕРИИ УСПЕХА

**Улучшенная система должна:**

✅ Блокировать вход при composite signal = HOLD с confidence < 0.5  
✅ Блокировать вход при низком volume для скальпинга (< 0.5 на 1m/5m)  
✅ Блокировать вход при bearish MACD на 2+ коротких TF для LONG  
✅ Корректно рассчитывать probability (не завышать)  
✅ Учитывать все факторы с правильными весами  
✅ Иметь обязательные hard stops  

**Результат:**
- ZEN/USDT должен быть BLOCKED новой системой
- Score должен быть 0.0 или очень низкий
- Probability должна быть 0% или очень низкая

---

## ВЫХОДНОЙ ФОРМАТ

**Создай файл:** `SYSTEM_IMPROVEMENTS_SIGNAL_ANALYSIS.md`

**Структура:**
```markdown
# 🔬 Анализ и Улучшение Системы Анализа Сигналов

## 1. АНАЛИЗ ТЕКУЩЕЙ РЕАЛИЗАЦИИ
[Детальный анализ каждого компонента]

## 2. НАЙДЕННЫЕ ПРОБЛЕМЫ
[Список проблем с корневыми причинами]

## 3. ПРЕДЛОЖЕННЫЕ УЛУЧШЕНИЯ
[Конкретный код улучшений]

## 4. ТЕСТИРОВАНИЕ
[Результаты на ZEN/USDT]

## 5. ПЛАН ВНЕДРЕНИЯ
[Пошаговый план]
```

---

## ФИНАЛЬНОЕ НАПОМИНАНИЕ

> **Цель:** Создать систему которая НЕ повторит ошибку ZEN/USDT.
> 
> **Метод:** Глубокий анализ → Найти корень проблемы → Конкретные улучшения → Валидация.
>
> **Результат:** Улучшенная система анализа сигналов с обязательными проверками.

---

**Версия:** 1.0  
**Дата:** 2025-11-18  
**Статус:** КРИТИЧЕСКИ ВАЖНО - ОБЯЗАТЕЛЬНО К ВЫПОЛНЕНИЮ

