# 🔧 Диагностика и Исправление MCP Инструментов Сканирования Рынка

## ПРОБЛЕМА

При попытке найти точки входа через протокол `market_analysis_protocol_FIXED.md` инструменты сканирования рынка из сервера `bybit-trading` недоступны или вызываются неправильно.

**Ожидаемые инструменты:**
- `scan_market` - умный сканер рынка
- `find_oversold_assets` - перепроданные активы (LONG)
- `find_overbought_assets` - перекупленные активы (SHORT)
- `find_breakout_opportunities` - возможности пробоя
- `find_trend_reversals` - развороты тренда

**Проблема:** Инструменты не вызываются или возвращают ошибки.

---

## ЗАДАЧА

1. **Диагностировать проблему:**
   - Проверить конфигурацию MCP серверов в Cursor
   - Определить правильный формат имен инструментов
   - Проверить доступность всех инструментов сканирования
   - Проверить логи сервера на ошибки

2. **Исправить проблему:**
   - Исправить формат вызова инструментов
   - Убедиться что все инструменты доступны
   - Протестировать каждый инструмент

3. **Обновить документацию:**
   - Обновить протокол анализа с правильными именами инструментов
   - Создать примеры правильного использования

---

## ШАГИ ДИАГНОСТИКИ

### ШАГ 1: Проверка Конфигурации MCP

**Файл:** `CURSOR_MCP_CONFIG.json` или настройки Cursor

**Проверить:**
```json
{
  "mcpServers": {
    "bybit-trading": {
      "command": "/Users/Gyber/GYBERNATY-ECOSYSTEM/TRADER-AGENT/venv/bin/python",
      "args": [
        "/Users/Gyber/GYBERNATY-ECOSYSTEM/TRADER-AGENT/mcp_server/full_server.py"
      ]
    }
  }
}
```

**Действия:**
1. Убедиться что сервер `bybit-trading` настроен
2. Проверить что путь к Python правильный
3. Проверить что путь к `full_server.py` правильный
4. Проверить что venv активирован и зависимости установлены

---

### ШАГ 2: Проверка Доступности Инструментов

**Попробовать вызвать инструменты в разных форматах:**

**Вариант 1: Без префикса**
```
scan_market
find_oversold_assets
find_overbought_assets
```

**Вариант 2: С префиксом сервера (дефис)**
```
bybit-trading_scan_market
bybit-trading_find_oversold_assets
```

**Вариант 3: С префиксом сервера (подчеркивание)**
```
bybit_trading_scan_market
bybit_trading_find_oversold_assets
```

**Вариант 4: Через MCP формат**
```
mcp_bybit-trading_scan_market
mcp_bybit-trading_find_oversold_assets
```

**Действия:**
1. Попробовать каждый вариант
2. Записать какой формат работает
3. Записать ошибки если есть

---

### ШАГ 3: Проверка Регистрации Инструментов

**Файл:** `mcp_server/full_server.py`

**Проверить:**
1. Инструменты зарегистрированы в `@app.list_tools()`:
   - `scan_market` (строка 367)
   - `find_oversold_assets` (строка 397)
   - `find_overbought_assets` (строка 409)
   - `find_breakout_opportunities` (строка 421)
   - `find_trend_reversals` (строка 433)

2. Инструменты обрабатываются в `@app.call_tool()`:
   - `scan_market` (строка 843)
   - `find_oversold_assets` (строка 850)
   - `find_overbought_assets` (строка 857)
   - `find_breakout_opportunities` (строка 864)
   - `find_trend_reversals` (строка 871)

**Действия:**
1. Прочитать секцию `@app.list_tools()` полностью
2. Прочитать секцию `@app.call_tool()` для этих инструментов
3. Убедиться что имена совпадают
4. Проверить что параметры правильные

---

### ШАГ 4: Проверка Зависимостей

**Файл:** `mcp_server/market_scanner.py`

**Проверить:**
1. Класс `MarketScanner` реализует все методы:
   - `scan_market()` (строка 19)
   - `find_oversold_assets()` (строка 712)
   - `find_overbought_assets()` (должен быть)
   - `find_breakout_opportunities()` (должен быть)
   - `find_trend_reversals()` (должен быть)

2. Все зависимости установлены:
   - `pandas`
   - `numpy`
   - `ta` (technical analysis library)
   - `ccxt` или `pybit`

**Действия:**
1. Проверить что все методы существуют
2. Проверить что они правильно вызываются из `full_server.py`
3. Убедиться что зависимости установлены в venv

---

### ШАГ 5: Тестирование Сервера Локально

**Команда:**
```bash
cd /Users/Gyber/GYBERNATY-ECOSYSTEM/TRADER-AGENT
source venv/bin/activate
python mcp_server/full_server.py
```

**Ожидаемый результат:**
- Сервер запускается без ошибок
- Показывает список доступных инструментов
- Готов принимать запросы через stdio

**Действия:**
1. Запустить сервер
2. Проверить логи на ошибки
3. Убедиться что все инструменты в списке

---

### ШАГ 6: Тестирование Через Cursor

**В новом чате попробовать:**

```
Попробуй вызвать инструмент scan_market с параметрами:
{
  "criteria": {
    "market_type": "spot",
    "min_volume_24h": 1000000,
    "indicators": {"rsi_range": [0, 35]},
    "direction": "long"
  },
  "limit": 10
}
```

**Попробовать все форматы:**
1. `scan_market` (без префикса)
2. `bybit-trading_scan_market`
3. `bybit_trading_scan_market`
4. `mcp_bybit-trading_scan_market`

**Действия:**
1. Записать какой формат работает
2. Записать ошибки если есть
3. Протестировать все 5 инструментов сканирования

---

## ИСПРАВЛЕНИЕ

### Если проблема в формате имен:

**Обновить протокол:** `prompts/market_analysis_protocol_FIXED.md`

Заменить все вызовы на правильный формат, например:
```python
# БЫЛО (неправильно):
mcp_bybit-trading_scan_market(...)

# СТАЛО (правильно):
scan_market(...)  # или другой формат который работает
```

### Если проблема в регистрации инструментов:

**Проверить `full_server.py`:**
1. Убедиться что все инструменты в `@app.list_tools()`
2. Убедиться что все инструменты в `@app.call_tool()`
3. Убедиться что имена совпадают точно

### Если проблема в реализации методов:

**Проверить `market_scanner.py`:**
1. Убедиться что все методы реализованы
2. Убедиться что они возвращают правильный формат
3. Убедиться что нет ошибок в логике

### Если проблема в зависимостях:

**Установить зависимости:**
```bash
cd /Users/Gyber/GYBERNATY-ECOSYSTEM/TRADER-AGENT
source venv/bin/activate
pip install -r requirements.txt
```

---

## ТЕСТИРОВАНИЕ

После исправления протестировать:

### Тест 1: scan_market
```
Вызови scan_market с критериями для поиска oversold активов (LONG)
```

### Тест 2: find_oversold_assets
```
Вызови find_oversold_assets для spot рынка с min_volume_24h = 1000000
```

### Тест 3: find_overbought_assets
```
Вызови find_overbought_assets для spot рынка с min_volume_24h = 1000000
```

### Тест 4: find_breakout_opportunities
```
Вызови find_breakout_opportunities для spot рынка
```

### Тест 5: find_trend_reversals
```
Вызови find_trend_reversals для spot рынка
```

### Тест 6: Полный анализ
```
Выполни полный анализ рынка по протоколу market_analysis_protocol_FIXED.md
Должно быть найдено минимум 3 LONG + 4 SHORT сигнала
```

---

## ОЖИДАЕМЫЙ РЕЗУЛЬТАТ

После исправления:

1. ✅ Все 5 инструментов сканирования доступны и работают
2. ✅ Правильный формат имен инструментов определен и задокументирован
3. ✅ Протокол анализа обновлен с правильными именами
4. ✅ Полный анализ рынка находит 3 LONG + 4 SHORT сигнала
5. ✅ Все инструменты возвращают данные в правильном формате

---

## ФАЙЛЫ ДЛЯ ПРОВЕРКИ

1. `CURSOR_MCP_CONFIG.json` - конфигурация MCP серверов
2. `mcp_server/full_server.py` - регистрация и обработка инструментов
3. `mcp_server/market_scanner.py` - реализация методов сканирования
4. `prompts/market_analysis_protocol_FIXED.md` - протокол анализа
5. `requirements.txt` - зависимости Python
6. `logs/trading_server_*.log` - логи сервера

---

## КРИТЕРИИ УСПЕХА

✅ Все инструменты сканирования вызываются без ошибок  
✅ Инструменты возвращают данные в правильном формате  
✅ Полный анализ рынка работает по протоколу  
✅ Найдено минимум 3 LONG + 4 SHORT сигнала  
✅ Документация обновлена с правильными именами  

---

**НАЧНИ С ШАГА 1 и пройди все шаги последовательно. Записывай результаты каждого шага.**
