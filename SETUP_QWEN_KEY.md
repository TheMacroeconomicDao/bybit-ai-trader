# 🔑 Настройка QWEN_API_KEY в переменных окружения

## ✅ Что сделано

Добавлена секция в `~/.zshrc` для постоянного хранения переменных окружения.

---

## 📝 Что нужно сделать

### 1. Откройте файл `.zshrc`:

```bash
nano ~/.zshrc
```

Или через любой редактор:
```bash
code ~/.zshrc  # VS Code
vim ~/.zshrc   # Vim
```

### 2. Найдите секцию:

```bash
# ==============================================================================
# TRADE COPILOT - Qwen API Configuration
# ==============================================================================
export QWEN_API_KEY="sk-or-v1-ВАШ_КЛЮЧ_СЮДА"
export AI_ENDPOINT="https://openrouter.ai/api/v1"
export AI_MODEL="qwen/qwen-turbo"
# ==============================================================================
```

### 3. Замените `sk-or-v1-ВАШ_КЛЮЧ_СЮДА` на ваш реальный ключ:

```bash
export QWEN_API_KEY="sk-or-v1-ваш-реальный-ключ-от-openrouter"
```

**Где взять ключ:**
- Откройте: https://openrouter.ai/keys
- Нажмите "Create Key" (если еще нет)
- Скопируйте ключ (формат: `sk-or-v1-...`)

### 4. Сохраните файл и примените изменения:

```bash
source ~/.zshrc
```

Или просто откройте новый терминал.

---

## ✅ Проверка

Проверьте что переменная установлена:

```bash
echo $QWEN_API_KEY
```

Должен показать ваш ключ (или `sk-or-v1-ВАШ_КЛЮЧ_СЮДА` если еще не заменили).

---

## 🚀 Готово!

Теперь переменная будет доступна:
- ✅ После перезагрузки системы
- ✅ Во всех новых терминалах
- ✅ В WebUI автоматически

**Просто замените placeholder на реальный ключ и всё!** 🔐


