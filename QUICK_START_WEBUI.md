# ⚡ Быстрый Старт: WebUI через MCP

## 🎯 Просто Скажи Агенту

**В Cursor просто напиши:**

```
"Открой UI"
```

или

```
"Покажи WebUI"
```

**Агент автоматически:**
1. ✅ Проверит, собран ли WebUI
2. ✅ Проверит, запущен ли HTTP сервер
3. ✅ Запустит сервер, если нужно
4. ✅ Откроет браузер с WebUI на `http://localhost:8081`

---

## 🔧 Настройка Автоматического Открытия

Если хочешь, чтобы WebUI открывался автоматически при запуске HTTP сервера:

### Вариант 1: Через Cursor Config

Отредактируй `CURSOR_MCP_CONFIG.json`:

```json
{
  "mcpServers": {
    "bybit-analysis": {
      "env": {
        "AUTO_OPEN_WEBUI": "true"
      }
    }
  }
}
```

### Вариант 2: Через Переменную Окружения

```bash
export AUTO_OPEN_WEBUI=true
```

---

## 📍 Порт

WebUI теперь работает на порту **8081** (вместо 8080), чтобы не конфликтовать с другими проектами.

Если нужно изменить порт:

```json
{
  "env": {
    "MCP_HTTP_PORT": "9000"
  }
}
```

---

## ✅ Всё Готово!

Просто скажи агенту "Открой UI" и всё заработает автоматически! 🚀










