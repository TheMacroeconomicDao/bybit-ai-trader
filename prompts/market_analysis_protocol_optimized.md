# 🔍 Протокол Анализа Рынка (ОПТИМИЗИРОВАННЫЙ)

## ⚠️ КРИТИЧЕСКАЯ ПРОБЛЕМА: Переполнение Контекста

**Проблема:**
- Протокол требует анализа сотен активов
- Каждый `analyze_asset` возвращает огромный объем данных (multi-TF, все индикаторы, паттерны)
- Дублирование анализа: `scan_market` уже делает `analyze_asset`, затем протокол требует еще раз
- Накопление всех данных в контексте → переполнение → невозможность завершить задачу

**Решение:**
- Использовать результаты `scan_market` напрямую (они уже содержат `analysis` и `score`)
- НЕ делать повторный `analyze_asset` для уже проанализированных активов
- Детальный анализ ТОЛЬКО для топ 3-5 кандидатов с score >= 8.0
- Сразу фильтровать и отбрасывать слабые результаты
- Использовать агрегированные данные для промежуточных этапов

---

## ОПТИМИЗИРОВАННЫЙ ПРОЦЕСС

### ШАГ 1: Быстрый Market Overview (2-3 минуты)

**Действия (ПАРАЛЛЕЛЬНО):**

```
1.1. BTC Status (быстро):
    → get_ticker("BTCUSDT", "spot")
    → get_kline("BTCUSDT", "240", 20) // только последние 20 свечей

1.2. Market Overview (агрегированные данные):
    → get_market_overview("spot", limit=100) // Используем Spot для безопасности!
    
1.3. Параллельный поиск (используем scan_market - он уже делает анализ):
    → scan_market({min_volume: 1M, min_score: 6, direction: "long", market_type: "spot"}, limit=20)
    → scan_market({min_volume: 1M, min_score: 6, direction: "short", market_type: "spot"}, limit=20)
    → find_oversold_assets(market_type="spot") // limit=15
    → find_breakout_opportunities(market_type="spot") // limit=15
    → find_trend_reversals(market_type="spot") // limit=15
```

**ВАЖНО:** 
- `scan_market` УЖЕ возвращает `analysis`, `score`, `probability`, `entry_plan`
- Score автоматически рассчитан по **10-факторной матрице** (Trend, BTC, Pattern, Volume, R:R, S/R, etc.)
- НЕ нужно пересчитывать Score вручную! Смотри `score_breakdown` для деталей.
- НЕ нужно делать `analyze_asset` повторно для этих результатов!
- Используем `limit=15-20` вместо 100, чтобы не перегружать контекст

**Output (краткий):**

```
📊 MARKET OVERVIEW

🔸 BTC: $50,250 (+1.2%)
   Trend: Uptrend на 4h

📈 Market Sentiment: Умеренно Бычий
   Green: 62% | Red: 38%

🔍 Найдено возможностей:
   📈 Long candidates: 12 (score >= 6.0)
   📉 Short candidates: 8 (score >= 6.0)
   - Oversold: 5
   - Breakouts: 3
   - Reversals: 4

⚠️ ВАЖНО: Показывай ОБА направления в финальном отчете!
```

---

### ШАГ 2: Агрегация и Первичная Фильтрация (1 минута)

**Действия:**

```
2.1. Объединить ВСЕ результаты из ШАГ 1:
     - scan_market результаты (уже содержат analysis!)
     - find_oversold_assets
     - find_breakout_opportunities
     - find_trend_reversals
     → Убрать дубликаты по symbol
     → ИТОГО: 30-50 уникальных активов

2.2. Быстрая фильтрация (используем УЖЕ рассчитанные score):
     - Score >= 7.0 (из scan_market)
     - Volume >= $1M
     - Probability >= 65% (из scan_market)
     → ОСТАЕТСЯ: 10-20 активов

2.3. Ранжирование:
     - Сортировать по score DESC
     - Топ 10 для дальнейшего анализа
```

**ВАЖНО:**
- НЕ вызываем `analyze_asset` здесь!
- Используем данные из `scan_market` результатов
- Отбрасываем слабые результаты сразу (не храним в контексте)

**Output (краткий список):**

```
📋 TOP CANDIDATES (Score >= 7.0)

1. SOL/USDT - Score: 8.5, Probability: 75%
2. ETH/USDT - Score: 8.0, Probability: 72%
3. AVAX/USDT - Score: 7.8, Probability: 70%
...
10. LINK/USDT - Score: 7.0, Probability: 65%
```

---

### ШАГ 3: Детальный Анализ ТОЛЬКО Топ-Кандидатов (5-7 минут)

**КРИТИЧЕСКИ ВАЖНО:** Детальный анализ ТОЛЬКО для топ 3-5 кандидатов с score >= 8.0!

**Действия (ТОЛЬКО для топ 3-5):**

```
3.1. Для каждого из топ 3-5:
     - Используем УЖЕ полученный analysis из scan_market
     - Если нужно больше деталей → analyze_asset ТОЛЬКО для этого актива
     - SMART MONEY ПРОВЕРКИ (Deep Dive):
       → CVD Analysis: Уже включен в analysis['cvd_analysis']
       → Order Blocks: Уже включены в analysis['timeframes']['4h']['order_blocks']
       → News Sentiment: Используй web_search("news [symbol] crypto") если сомневаешься
     - Дополнительные проверки (если нужно):
       → get_support_resistance() // только для финальных кандидатов
       → get_market_structure() // только если критично

3.2. Review Score & Confluence:
     - Score уже рассчитан по матрице (см. score_breakdown)
     - Проверяем CVD: если Divergence в сторону сделки → +Confidence
     - Проверяем OB: если цена в зоне Order Block → +Confidence
     - Убеждаемся что score >= 8.0
     - Проверяем чеклист из zero_risk_methodology для финального подтверждения
     - Minimum 8.0/10 для рекомендации
```

**ВАЖНО:**
- НЕ делаем `analyze_asset` для всех 20-30 кандидатов!
- ТОЛЬКО для топ 3-5 с score >= 8.0
- Используем уже полученные данные из `scan_market`

**Output (детальный для топ 3-5):**

```
═══════════════════════════════════════
🎯 ТОП ВОЗМОЖНОСТИ
═══════════════════════════════════════

📈 LONG ВОЗМОЖНОСТИ:

━━━━ LONG #1 ━━━━

💎 SOL/USDT - Score: 8.5/10 ⭐
💵 Цена: $145.50 (+8.5%)

📊 АНАЛИЗ (из scan_market results):
   - Multi-TF: 4/4 aligned ✅
   - Indicators: 7/7 confirmed ✅
   - Pattern: Bull Flag (78% success)
   - Volume: 2.3x average ✅
   - BTC: Supports ✅

🔍 CONFLUENCE: 8.5/10
📊 Probability: 75%
⚡ R:R: 1:2.5

✅ РЕКОМЕНДАЦИЯ: ОТКРЫВАТЬ

📉 SHORT ВОЗМОЖНОСТИ:

━━━━ SHORT #1 ━━━━

🔴 ETH/USDT - Score: 7.8/10 ⚠️
💵 Цена: $2,450 (-3.2%)

📊 АНАЛИЗ (из scan_market results):
   - Multi-TF: 3/4 aligned ✅
   - Indicators: 6/7 confirmed ✅
   - Pattern: Bear Flag (72% success)
   - Volume: 1.8x average ✅
   - BTC: Supports SHORT ✅

🔍 CONFLUENCE: 7.8/10
📊 Probability: 68%
⚡ R:R: 1:2.0

⚠️ РЕКОМЕНДАЦИЯ: ОСТОРОЖНО (score < 8.0, но BTC в downtrend)
```

---

### ШАГ 4: Финальная Самопроверка и Презентация (2 минуты)

**Действия:**

```
4.1. Самопроверка (чеклист из zero_risk_methodology):
     - Только для финальных кандидатов (топ 3-5)
     - Minimum 15/17 ✅ для рекомендации

4.2. Презентация результатов:
     - Только качественные setups (score >= 8.0)
     - Если нет → честно сказать "нет безопасных возможностей"
```

---

## КЛЮЧЕВЫЕ ОПТИМИЗАЦИИ

### 1. Использование Результатов scan_market

**БЫЛО (неэффективно):**
```
scan_market() → получаем список
→ для каждого: analyze_asset() // ДУБЛИРОВАНИЕ!
→ для каждого: get_kline() на всех TF
→ для каждого: get_ml_rsi()
→ накопление огромного объема данных
```

**СТАЛО (оптимизировано):**
```
scan_market() → получаем список с УЖЕ готовым analysis
→ используем analysis напрямую
→ дополнительный анализ ТОЛЬКО для топ 3-5
→ минимальное накопление данных
```

### 2. Ограничение Количества Активов

**БЫЛО:**
- УРОВЕНЬ 1: 200-500 активов
- УРОВЕНЬ 2: 50-100 активов
- УРОВЕНЬ 3: 20-30 активов с analyze_asset

**СТАЛО:**
- ШАГ 1: 50-80 активов (из scan_market + find_*)
- ШАГ 2: 10-20 активов (фильтрация по score)
- ШАГ 3: 3-5 активов (детальный анализ)

### 3. Немедленная Фильтрация

**Правило:** Отбрасывать слабые результаты сразу, не хранить в контексте.

```
Если score < 7.0 → исключить сразу
Если probability < 65% → исключить сразу
Если volume < $1M → исключить сразу
```

### 4. Использование Агрегированных Данных

**Вместо детального анализа для всех:**
- Используем `score` и `probability` из `scan_market`
- Используем `entry_plan` из `scan_market`
- Используем `analysis` из `scan_market` (уже содержит multi-TF данные)

**Детальный анализ ТОЛЬКО когда:**
- Score >= 8.0
- В топ 3-5 кандидатов
- Нужны дополнительные проверки

---

## ПРАВИЛА РАБОТЫ С КОНТЕКСТОМ

### ✅ ДЕЛАТЬ:

1. **Использовать результаты scan_market напрямую**
   - Они уже содержат `analysis`, `score`, `probability`
   - НЕ делать повторный `analyze_asset`

2. **Фильтровать сразу**
   - Отбрасывать слабые результаты (score < 7.0)
   - Не хранить в контексте промежуточные данные

3. **Ограничивать количество**
   - `scan_market` с `limit=15-20` вместо 100
   - Детальный анализ ТОЛЬКО для топ 3-5

4. **Использовать агрегированные данные**
   - `score`, `probability` вместо полного анализа
   - `entry_plan` вместо ручного расчета

### ❌ НЕ ДЕЛАТЬ:

1. **НЕ дублировать анализ**
   - НЕ вызывать `analyze_asset` для активов из `scan_market`
   - НЕ делать `get_kline` для всех активов на всех TF

2. **НЕ накапливать все данные**
   - НЕ хранить результаты для всех 100+ активов
   - НЕ сохранять промежуточные вычисления

3. **НЕ анализировать все подряд**
   - НЕ делать детальный анализ для 20-30 активов
   - НЕ проверять все таймфреймы для всех активов

---

## ПРИМЕР ОПТИМИЗИРОВАННОГО ПРОЦЕССА

```
ШАГ 1: Быстрый Overview (2 мин)
├─ get_ticker("BTCUSDT") // 1 вызов
├─ get_market_overview(limit=100) // 1 вызов
├─ scan_market(long, limit=20) // 1 вызов → 20 активов с analysis
├─ scan_market(short, limit=20) // 1 вызов → 20 активов с analysis
├─ find_oversold_assets(limit=15) // 1 вызов → 15 активов
├─ find_breakout_opportunities(limit=15) // 1 вызов → 15 активов
└─ find_trend_reversals(limit=15) // 1 вызов → 15 активов

ИТОГО: 7 вызовов, ~70-85 активов (с дубликатами)

ШАГ 2: Фильтрация (1 мин)
├─ Объединение результатов → 30-50 уникальных
├─ Фильтрация по score >= 7.0 → 10-20 активов
└─ Ранжирование → топ 10

ИТОГО: 0 дополнительных вызовов (работа с данными)

ШАГ 3: Детальный анализ (5 мин)
├─ Для топ 3-5 с score >= 8.0:
│  ├─ Используем analysis из scan_market
│  ├─ get_support_resistance() // только если нужно
│  └─ Confluence scoring
└─ Для остальных: используем данные из scan_market

ИТОГО: 3-5 дополнительных вызовов (только для топ кандидатов)

ШАГ 4: Презентация (2 мин)
└─ Самопроверка и финальный вывод

ОБЩИЙ ИТОГ: ~10-12 вызовов вместо 50-100+
```

---

## КРИТЕРИИ ОСТАНОВКИ

**Остановиться и представить результаты если:**

1. Найдено 2-3 кандидата с score >= 8.5
2. Проанализировано 50+ активов (из scan_market)
3. Топ 5 кандидатов определены
4. Нет кандидатов с score >= 8.0 после проверки 50+ активов

**НЕ продолжать если:**
- Уже нашли качественные setups (score >= 8.5)
- Контекст начинает переполняться
- Нет улучшения после проверки 50+ активов

---

## ЗАКЛЮЧЕНИЕ

**Этот оптимизированный протокол:**
- ✅ Устраняет переполнение контекста
- ✅ Устраняет дублирование анализа
- ✅ Использует уже полученные данные
- ✅ Фокусируется на качественных setups
- ✅ Завершает задачу в рамках одного контекста

**Ключевой принцип:**
> Используй то, что уже есть.  
> Анализируй детально только лучшее.  
> Отбрасывай слабое сразу.

---

*Версия 2.0 - Оптимизирован для предотвращения переполнения контекста*














